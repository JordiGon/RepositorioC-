import { createTheme } from "@material-ui/core";

const theme = createTheme({
  palette: {
    primary: {
      light: "#004589",
      main: "#004589",
      dark: "#004589",
      contrastText: "#ECFAD8",
    },
  },
});

export default theme;
